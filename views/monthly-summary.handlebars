<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monthly Summary</title>
       <link rel="stylesheet" href="/style.css">
</head>
<body>

       <nav>
        <ul>
            {{#if userId}} <!-- If session exists (user is logged in) -->
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/monthly-summary">Monthly Summary</a></li>
                <li><a href="/yearly-summary">Yearly Summary</a></li>
                <li><a href="/logout">Logout</a></li>
            {{else}} <!-- If no session (user is not logged in) -->
                <li><a href="/login">Login</a></li>
                <li><a href="/signup">Signup</a></li>
            {{/if}}
        </ul>
    </nav>
    <h1>Monthly Summary for {{month}}/{{year}}</h1>

    {{#if message}}
        <p>{{message}}</p>
    {{else}}

        <table>
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Category</th>
                    <th>Amount</th>
                    <th>Date</th>
                </tr>
            </thead>
            <tbody>
                {{#each monthlyExpenses}}
                    <tr>
                        <td>{{this.description}}</td>
                        <td>{{this.category}}</td>
                        <td>${{this.amount}}</td>
                        <td>{{ formatDate this.date}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>

        <h3>Total Expenses for {{month}}/{{year}}: ${{grandTotal}}</h3>

    {{/if}}

    <footer>
        <p>&copy; 2025 Expense Tracker</p>
    </footer>
</body>
</html>
